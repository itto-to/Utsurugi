using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class MakeMirror : MonoBehaviour {

	// Use this for initialization
	void Start () {
		
	}
	
	// Update is called once per frame
	void Update () {
		
	}

    public void Make(bool mode)
    {
        if (mode == true)
        {
            /*if (stageInfo.GetComponent<StageInfo>().DownMap[mPosX + mPosY * 10] == true)
            {

            }*/
        }
    }
}
